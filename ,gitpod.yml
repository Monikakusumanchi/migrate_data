image:
  file: .gitpod.Dockerfile

tasks:
  - init: |
      docker-compose up -d
      pip install -r requirements.txt
  - command: |
      python migrate.py

ports:
  - port: 1433
    onOpen: ignore
  - port: 5432
    onOpen: ignore
